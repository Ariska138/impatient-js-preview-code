let assert;_644‍.x([["code_const",()=>code_const],["code_let",()=>code_let]]);_644‍.w("assert",[["strict",["assert"],function(v){assert=v}]]);

       function code_const () {
    const i = 0;

    assert.throws(
        () => { i = i + 1},
        {
            name: 'TypeError',
            message: 'Assignment to constant variable.'
        }
    );
}

       function code_let () {
    let i;

    i = 0;
    i = i + 1;

    assert.equal(i, 1);
}

